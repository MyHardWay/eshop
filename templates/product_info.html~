{% extends 'base.html' %}
{% load staticfiles %}
{% block Main %}
<div class = "small-8 small-centered columns" style="padding-top: 10px">
	<div class="small-10 columns">
	<h2>{{product.author}} : {{product.title}}</h2>
		<div class="small-3 columns">			
			<img src="../../static/{{product.get_picture}}">
		</div>
	        <div class="small-4 end columns">
			<p>{{product.text}}</p>
	        </div>
	</div>
</div>

{% endblock %}
